# Recording-only render settings - tuned via preview GUI
# Use with: python -m overlay_render --folder <path> --settings this_file.yaml --recording-only

view:
  method: percentile
  p_lo: 39
  p_hi: 100
  gamma: 1.3
  clahe: false
